"""REST route + HTTP client extraction (Phase 1).

Will produce ``:Route``, ``:HttpClientCall``, and ``:HTTP_FLOW`` graph elements.
Frameworks targeted by MVP: FastAPI, Flask, Django (Python); Express, Next.js,
NestJS (TS/JS).
"""

__all__: list[str] = []
